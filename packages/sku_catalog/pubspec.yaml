name: sku_catalog
description: >-
  A catalog model for physical parts: SKUs with traits and typed attributes,
  assemblies of SKUs, the manufacturers behind them, and the places they go.
version: 0.1.0
repository: https://github.com/taybiz/sku_catalog
issue_tracker: https://github.com/taybiz/sku_catalog/issues
topics:
  - catalog
  - sku
  - inventory
  - domain-model
  - traits
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  equatable: ^2.0.7
  fpdart: ^1.2.0

dev_dependencies:
  lints: ^6.1.0
  shouldly: ^0.5.0+1
  test: ^1.25.0
