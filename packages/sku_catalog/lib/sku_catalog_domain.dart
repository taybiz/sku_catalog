/// The catalog model alone — entities, value objects, enums, failures and
/// repository contracts, with no operations.
///
/// Import this instead of `sku_catalog.dart` when you are implementing a
/// repository backend or your own application layer and do not want the
/// bundled use cases in scope.
library;

export 'src/domain/domain.dart';
