name: sku_catalog_domain
description: >-
  The catalog model: traits with typed attributes, SKUs, assemblies of SKUs,
  manufacturers, places (locates), devices, and the repository contracts.
version: 0.1.0
repository: https://github.com/taybiz/sku_catalog
issue_tracker: https://github.com/taybiz/sku_catalog/issues
topics:
  - catalog
  - sku
  - domain-model
  - attributes
  - traits
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  equatable: ^2.0.7
  fpdart: ^1.2.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
