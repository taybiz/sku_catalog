name: sku_catalog_usecases
description: >-
  Operations for the sku_catalog model: CRUD, trait-chain and attribute-schema
  resolution, assembly edges with a cycle guard, and the device and place rules.
version: 0.1.0
repository: https://github.com/taybiz/sku_catalog
issue_tracker: https://github.com/taybiz/sku_catalog/issues
topics:
  - catalog
  - sku
  - usecases
  - application-layer
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  sku_catalog_domain: ^0.1.0
  equatable: ^2.0.7
  fpdart: ^1.2.0

dev_dependencies:
  lints: ^6.1.0
  shouldly: ^0.5.0+1
  test: ^1.25.0
  sku_catalog_memory: ^0.1.0
